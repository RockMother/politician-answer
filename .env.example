# Required
BOT_TOKEN=your-telegram-bot-token
OPENAI_API_KEY=your-openai-api-key

# Optional — OpenAI model (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Optional — custom system prompt (overrides config/prompt.txt and built-in default)
# OPENAI_SYSTEM_PROMPT=You are a debater...

# Optional — path to a prompt file (default: config/prompt.txt)
# PROMPT_FILE=config/prompt.txt

# Optional — comma-separated Telegram user IDs allowed to use /setprompt
# ADMIN_USER_IDS=123456789,987654321

# Optional — port for the health-check HTTP server (default: 3000)
# PORT=3000
